# Copy to .env.production for production runs (npm start).

# MONGO_URI=mongodb+srv://USER:PASS@cluster.example.mongodb.net/jitox
# PORT=4000
# JWT_SECRET_KEY=use-a-long-random-secret
# CORS_ORIGINS=https://jitox.vercel.app

# Gmail SMTP — sent directly from code (nodemailer), no Resend/SendGrid
EMAIL_USER=shubhamradadiya@gmail.com
EMAIL_PASS=your-16-char-gmail-app-password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
# EMAIL_SENDER_NAME=Jitox Agro

# App password: https://myaccount.google.com/apppasswords (2-Step Verification required)
# Render FREE blocks SMTP ports — use paid Render or another host for production mail
