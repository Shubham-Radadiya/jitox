# Copy to .env.local (gitignored) for local OTP tests.

EMAIL_USER=shubhamradadiya@gmail.com

# Production on Render: Gmail API (HTTPS) — same as Render env
EMAIL_TRANSPORT=auto
# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=
# GMAIL_REFRESH_TOKEN=

# Local SMTP (optional; blocked on Render)
# EMAIL_PASS=your-16-char-gmail-app-password
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false

# Dev: log OTP in console when email is not configured
# EXPOSE_OTP_IN_DEV=true
