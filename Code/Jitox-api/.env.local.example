# Copy to .env.local (gitignored) for local OTP email tests.

# Render production (free tier) — use Resend instead of SMTP:
# RESEND_API_KEY=re_xxxxxxxx
# RESEND_FROM=Jitox Agro <onboarding@resend.dev>

# Local / paid Render — Gmail SMTP:
EMAIL_USER=shubhamradadiya@gmail.com
EMAIL_PASS=your-gmail-16-char-app-password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
