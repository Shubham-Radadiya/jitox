# Copy to .env.local (or .env) — Vite exposes only variables prefixed with VITE_.

VITE_API_BASE_URL=http://localhost:4000

# Public site URL (no trailing slash) — used for canonical, Open Graph, Twitter cards,
# and generated robots.txt / sitemap.xml on production build.
VITE_SITE_URL=https://jitox.com

# Optional: Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX)
# VITE_GA_MEASUREMENT_ID=

# Optional: Google Search Console HTML tag verification content value only
# VITE_GSC_VERIFICATION=
